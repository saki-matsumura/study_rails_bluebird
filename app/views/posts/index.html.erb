<h2>ホーム画面</h2>

<%= link_to 'ポストする', new_post_path %>

<table>
  <tr>
     <td><p>つぶやき</p></td>
  </tr>

</table>
<table>
  <% @posts.each do |post| %>
    <tr>
       <td><%= post.content %></td>
       <td><%= link_to '編集', edit_post_path(post.id) %></td>
    </tr>
  <% end %>
</table>

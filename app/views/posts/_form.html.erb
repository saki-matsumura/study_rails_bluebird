<%= form_with(model: @post, local: true, url: choose_new_or_edit) do |form| %>

<div class="post_content">
  <%= form.label :content %>
      <%= form.text_field :content %>
</div>
<%= form.submit %>
<% end %>

<%= link_to "ホームにもどる", posts_path %>